✅ What You Should Do:
Option 1: Use Git Bash (Easy + Recommended for Hive)
If you have Git Bash installed (comes with Git for Windows):

Open Git Bash

Navigate to your Hive folder:

bash
Copy
Edit
cd /c/path/to/hive
Run:

bash
Copy
Edit
./bin/schematool -initSchema -dbType derby